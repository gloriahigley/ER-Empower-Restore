<%= form_for(@routing) do |f| %>
  <% if @routing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@routing.errors.count, "error") %> prohibited this routing from being saved:</h2>

      <ul>
      <% @routing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :possible_id %><br>
    <%= f.number_field :possible_id %>
  </div>
  <div class="field">
    <%= f.label :next_question_id %><br>
    <%= f.number_field :next_question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
